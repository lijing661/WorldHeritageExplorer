# Uncomment the next line to define a global platform for your project
platform :ios, '16.0'

target 'WorldHeritageExplorer' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Imapge loading libraries
  pod 'Kingfisher', '~> 7.0' # 图片加载
  pod 'CSV.swift', '~> 2.4'  # CSV解析

  target 'WorldHeritageExplorerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'WorldHeritageExplorerUITests' do
    # Pods for testing
  end

end
